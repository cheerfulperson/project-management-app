import { ResizeHeightDirective } from './resize-height.directive';

describe('ResizeHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeHeightDirective();
    expect(directive).toBeTruthy();
  });
});
